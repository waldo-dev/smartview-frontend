# Variables de entorno para docker-compose
# Copia este archivo a .env y ajusta los valores

# ============================================
# BASE DE DATOS (EXTERNA - OTRO CONTENEDOR)
# ============================================
# Opción 1: Si está en otro docker-compose, usa el nombre del servicio:
# DB_HOST=nombre-del-servicio-postgres-del-otro-docker-compose
#
# Opción 2: Si es un contenedor independiente, usa el nombre del contenedor:
# DB_HOST=nombre-del-contenedor-postgres
#
# Opción 3: Si está en el host (puerto expuesto), usa:
# DB_HOST=host.docker.internal
#
# Para encontrar el nombre del contenedor: docker ps
DB_HOST=host.docker.internal
DB_NAME=smartview_db
DB_USER=smartview_db
DB_PASSWORD=sm4rtv13w
DB_PORT=5432

# ============================================
# BACKEND
# ============================================
PORT=5000
NODE_ENV=production
JWT_SECRET=tu-secret-jwt-super-seguro-cambiar-en-produccion

# ============================================
# POWER BI / AZURE AD
# ============================================
AZURE_CLIENT_ID=tu-client-id-de-azure
AZURE_CLIENT_SECRET=tu-client-secret-de-azure
AZURE_TENANT_ID=tu-tenant-id-de-azure
POWERBI_WORKSPACE_ID=tu-workspace-id
POWERBI_SCOPE=https://analysis.windows.net/powerbi/api/.default

# ============================================
# FRONTEND
# ============================================
# Nota: VITE_API_URL se configura automáticamente en docker-compose.yml
# Si necesitas cambiarla, edita docker-compose.yml directamente
# VITE_API_URL=http://backend:5000/api
